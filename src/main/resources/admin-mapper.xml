<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mubatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Admin">
    <!-- namespace的值是UserMapper的全限定名FQN -->
    <insert id="create" parameterType="demo.model.Admin" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO mybatis.student VALUES (NULL,#{email},#{username},#{password},#{role})
    </insert>

</mapper>